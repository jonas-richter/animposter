/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Character images are loaded as plain <img> with an onError fallback,
  // so no next/image remote patterns are required.
};

export default nextConfig;
